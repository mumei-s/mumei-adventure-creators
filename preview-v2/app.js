const NOTE_PROFILE='https://note.com/ss_yr/portal';
const RECRUIT='https://note.com/ss_yr/n/nf34d092b4032';
const MAGAZINE='https://note.com/ss_yr/m/mf8a55ef40b9d';
const app=document.getElementById('app'),modal=document.getElementById('modal'),toast=document.getElementById('toast'),notice=document.getElementById('notice');
const creators=[
{id:1,name:'無名S note①',title:'光庭のアイテムマイスター',job:'道具屋（アイテムマイスター）',rarity:'SR',attrs:['光','商才','癒し'],images:['assets/creator-1.jpg','assets/creator-1.jpg','assets/creator-1.jpg'],intro:'つながりや応援を大切にする、柔らかな世界観の見本クリエイター。見本名は他の参加者と重ならないように設定しています。',wins:17,views:642,favorites:82,articles:[['募集記事を見る',RECRUIT],['専用マガジンを見る',MAGAZINE]],stats:{HP:118,攻撃:54,防御:62,魔力:84,素早さ:67,幸運:73},skills:['おすすめピックアップ','アイテム鑑定眼','感謝の輪']},
{id:2,name:'無名S note②',title:'緩光の共創魔導士',job:'共創魔導士',rarity:'Dream SSR',attrs:['光','言霊','支援'],images:['assets/creator-2.jpg','assets/creator-2.jpg','assets/creator-2.jpg'],intro:'言葉と光で交流をひらく見本クリエイター。紹介・継続・共感を中心にした支援型カードです。',wins:26,views:778,favorites:111,articles:[['募集記事を見る',RECRUIT],['専用マガジンを見る',MAGAZINE]],stats:{HP:128,攻撃:63,防御:60,魔力:95,素早さ:80,幸運:86},skills:['記事コンパス','継続ブースト','星彩綴記']},
{id:3,name:'無名S note③',title:'黒焔の挑戦記録者',job:'狂戦士（バーサーカー）',rarity:'Legend SSR',attrs:['闇','炎','挑戦'],images:['assets/creator-3.jpg','assets/creator-3.jpg','assets/creator-3.jpg'],intro:'行動力と熱量を表現した挑戦型の見本クリエイター。対戦では高い攻撃力を持つサンプルです。',wins:25,views:813,favorites:106,articles:[['募集記事を見る',RECRUIT],['専用マガジンを見る',MAGAZINE]],stats:{HP:132,攻撃:87,防御:74,魔力:48,素早さ:59,幸運:61},skills:['バーサクライティング','共鳴ドライブ','黒焔ノート・ブレイズ']}
];
let submissions=[
{id:1,name:'申請見本A',title:'月影の短編蒐集者',job:'物語採集家',image:'assets/creator-1.jpg',date:'2026/08/06 11:20',status:'pending',magazine:true},
{id:2,name:'申請見本B',title:'風景を綴る旅人',job:'旅の記録士',image:'assets/creator-2.jpg',date:'2026/08/06 10:12',status:'pending',magazine:true},
{id:3,name:'申請見本C',title:'知識を灯す研究者',job:'学びの錬金術師',image:'assets/creator-3.jpg',date:'2026/08/05 22:41',status:'revision',magazine:false}
];
let state={page:(location.hash||'#home').slice(1),search:'',sort:'number',attribute:'all',ownerTab:'pending'};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function go(page){state.page=page;history.replaceState(null,'',page==='home'?location.pathname:'#'+page);window.scrollTo({top:0,behavior:'smooth'});render()}
function button(text,page,cls=''){return `<button class="btn ${cls}" data-page="${page}">${text}</button>`}
function card(c){return `<article class="creator-card" data-creator="${c.id}" tabindex="0"><figure><img src="${c.images[0]}" alt="${esc(c.name)}"><span class="rarity">${esc(c.rarity)}</span></figure><div class="card-body"><small>ARCHIVE No.${String(c.id).padStart(3,'0')}</small><h3>${esc(c.name)}</h3><p>${esc(c.job)}</p><div class="tags">${c.attrs.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div></article>`}
function home(){return `<section class="page"><section class="hero"><div><div class="hero-status"><span class="pulse"></span>確認用プレビュー</div><p class="eyebrow">MUMEI S NOTE · CREATOR ARCHIVE</p><h1>まだ知らない才能と、<br><em>カードで出会う。</em></h1><p class="lead">クリエイター一人ひとりに専用ページを用意。3枚のイラストと2本のnote記事を紹介し、気になった相手とはカード対戦を楽しめます。</p><div class="actions">${button('名鑑を開く','directory','primary')}${button('見本バトル','battle')}<a class="btn" href="${RECRUIT}" target="_blank" rel="noopener">参加方法を見る</a></div></div><div class="hero-showcase"><article class="main-card" data-creator="1"><img src="${creators[0].images[0]}" alt=""><div class="caption"><span>FEATURED SAMPLE 01</span><b>${esc(creators[0].name)}</b><small>${esc(creators[0].job)}</small></div></article><div class="mini-stack">${creators.map((c,i)=>`<button class="mini-card" data-creator="${c.id}"><img src="${c.images[0]}" alt=""><div><span>#${i+1}</span><b>${esc(c.name)}</b></div></button>`).join('')}</div></div></section><div class="stat-band"><div><b>3</b><span>見本クリエイター</span></div><div><b>3枚</b><span>1人のイラスト</span></div><div><b>2記事</b><span>note記事上限</span></div><div><b>承認制</b><span>オーナー公開管理</span></div></div><section class="section"><div class="section-head"><div><p class="eyebrow">FEATURED CREATOR</p><h2>見本カード</h2></div>${button('参加者一覧へ','directory')}</div><div class="creator-grid">${creators.map(card).join('')}</div></section><section class="section"><div class="section-head"><div><p class="eyebrow">OWNER CONTROL</p><h2>オーナー管理も見本で確認</h2></div>${button('管理画面を開く','owner','primary')}</div><div class="creator-grid"><div class="panel"><h3>申請通知</h3><p class="lead">新規申請が届くと右上と管理画面へ件数を表示。</p></div><div class="panel"><h3>承認・修正依頼</h3><p class="lead">内容を確認し、公開承認または修正依頼を選択。</p></div><div class="panel"><h3>マガジン追加管理</h3><p class="lead">専用マガジン追加希望の有無を一覧で確認。</p></div></div></section></section>`}
function directory(){const attrs=['all',...new Set(creators.flatMap(c=>c.attrs))];let list=creators.filter(c=>{const text=[c.name,c.title,c.job,...c.attrs,...c.articles.flat()].join(' ').toLowerCase();return(!state.search||text.includes(state.search.toLowerCase()))&&(state.attribute==='all'||c.attrs.includes(state.attribute))});const sort={number:(a,b)=>a.id-b.id,name:(a,b)=>a.name.localeCompare(b.name,'ja'),wins:(a,b)=>b.wins-a.wins,views:(a,b)=>b.views-a.views,favorites:(a,b)=>b.favorites-a.favorites};list.sort(sort[state.sort]);return `<section class="page"><header class="page-title"><p class="eyebrow">CREATOR DIRECTORY</p><h1>参加者一覧</h1><p class="lead">クリエイター名・職業・属性・記事タイトルを検索できます。</p></header><div class="toolbar panel"><input class="field" id="search" value="${esc(state.search)}" placeholder="名前・職業・属性・記事タイトル"><select class="field" id="attr">${attrs.map(a=>`<option value="${a}" ${a===state.attribute?'selected':''}>${a==='all'?'すべての属性':a}</option>`).join('')}</select><select class="field" id="sort"><option value="number" ${state.sort==='number'?'selected':''}>番号順</option><option value="name" ${state.sort==='name'?'selected':''}>名前順</option><option value="views" ${state.sort==='views'?'selected':''}>閲覧順</option><option value="favorites" ${state.sort==='favorites'?'selected':''}>お気に入り順</option><option value="wins" ${state.sort==='wins'?'selected':''}>勝利数順</option></select></div><div class="creator-grid">${list.length?list.map(card).join(''):'<div class="empty panel">該当するクリエイターはいません。</div>'}</div></section>`}
function profile(c){return `<div class="dialog-shell"><button class="close" data-close>×</button><div class="profile-top"><div><img class="gallery-main" src="${c.images[0]}" alt=""><div class="gallery-row">${c.images.map(x=>`<img src="${x}" alt="">`).join('')}</div></div><div class="profile-summary"><p class="eyebrow">ARCHIVE No.${String(c.id).padStart(3,'0')}</p><h2>${esc(c.name)}</h2><p class="sub">${esc(c.title)}｜${esc(c.job)}</p><div class="tags">${c.attrs.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div><p class="lead">${esc(c.intro)}</p><div class="stats">${[['レア度',c.rarity],['閲覧',c.views],['お気に入り',c.favorites],['勝利数',c.wins]].map(([a,b])=>`<div><b>${b}</b><span>${a}</span></div>`).join('')}</div><div class="article-links">${c.articles.map(a=>`<a class="article" href="${a[1]}" target="_blank" rel="noopener"><b>${esc(a[0])}</b><span>noteへ →</span></a>`).join('')}</div><div class="actions"><a class="btn" href="${NOTE_PROFILE}" target="_blank" rel="noopener">noteプロフィール</a><button class="primary" data-battle="${c.id}">このカードと対戦</button></div></div></div><section class="section panel"><p class="eyebrow">BATTLE STATUS</p><h3>ステータス・固有スキル</h3><div class="stats">${Object.entries(c.stats).map(([a,b])=>`<div><b>${b}</b><span>${a}</span></div>`).join('')}</div><div class="tags">${c.skills.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></section></div>`}
function fighter(c){return `<div class="fighter"><img src="${c.images[0]}" alt=""><b>${esc(c.name)}</b><p class="lead">${esc(c.job)}</p><div class="tags">${c.attrs.map(x=>`<span class="tag">${esc(x)}</span>`).join('')}</div></div>`}
function battle(){const ranking=[...creators].sort((a,b)=>b.wins-a.wins);return `<section class="page"><header class="page-title"><p class="eyebrow">CREATOR CARD BATTLE</p><h1>冒険カード対戦</h1><p class="lead">自分のカードと対戦相手を選び、見本の3ターン対戦を試せます。</p></header><div class="panel"><div class="battle-select"><div><h3>自分のカード</h3><select class="field" id="player">${creators.map(c=>`<option value="${c.id}">${esc(c.name)}｜${esc(c.job)}</option>`).join('')}</select><div id="playerView">${fighter(creators[0])}</div></div><div class="vs">VS</div><div><h3>対戦相手</h3><select class="field" id="opponent">${creators.map((c,i)=>`<option value="${c.id}" ${i===1?'selected':''}>${esc(c.name)}｜${esc(c.job)}</option>`).join('')}</select><div id="opponentView">${fighter(creators[1])}</div></div></div><div class="notice-box" style="margin-top:20px">見本版は簡易判定です。本番ではサーバー側で能力値・勝敗・履歴を処理し、改ざんを防止します。</div><div class="actions"><button class="primary" id="start">この組み合わせで対戦</button></div></div><section class="section"><div class="section-head"><div><p class="eyebrow">BATTLE RANKING</p><h2>対戦ランキング見本</h2></div></div><div class="rank-list">${ranking.map((c,i)=>`<div class="rank-item"><strong>${i+1}</strong><img src="${c.images[0]}" alt=""><div><b>${esc(c.name)}</b><small>${esc(c.job)}</small></div><strong>${c.wins}勝</strong></div>`).join('')}</div></section></section>`}
function apply(){return `<section class="page"><header class="page-title"><p class="eyebrow">JOIN THE DIRECTORY</p><h1>名鑑参加申請</h1><p class="lead">各クリエイターが自分で申請し、オーナー承認後に公開される想定の見本フォームです。</p></header><form class="panel" id="applyForm"><div class="form-grid"><label><span>クリエイター名</span><input class="field" required placeholder="noteで使用している名前"></label><label><span>職業名</span><input class="field" required placeholder="例：共創魔導士"></label><label><span>自己紹介</span><textarea class="field" rows="5" required placeholder="活動内容・得意分野・世界観"></textarea></label><label><span>noteプロフィールURL</span><input class="field" placeholder="https://note.com/..."></label><label><span>属性</span><input class="field" placeholder="光、言霊、支援など"></label><label><span>イラスト①</span><input class="field" type="file" accept="image/*"></label><label><span>イラスト②</span><input class="field" type="file" accept="image/*"></label><label><span>イラスト③</span><input class="field" type="file" accept="image/*"></label><label><span>note記事①</span><input class="field" placeholder="記事URL"></label><label><span>note記事②</span><input class="field" placeholder="記事URL"></label></div><div class="notice-box" style="margin-top:18px">掲載上限：イラスト3枚・note記事2本。変更申請もオーナー承認後に公開されます。</div><label class="check"><input type="checkbox" checked><span>専用マガジンへの追加を希望する</span></label><label class="check"><input type="checkbox" checked><span>カード対戦へ参加する</span></label><label class="check"><input type="checkbox" required><span>画像・文章の利用権限を持ち、名鑑と対戦演出での利用に同意する</span></label><button class="primary" type="submit">承認申請を送る</button></form></section>`}
function owner(){const current=submissions.filter(s=>s.status===state.ownerTab);const pending=submissions.filter(s=>s.status==='pending').length,revision=submissions.filter(s=>s.status==='revision').length,approved=submissions.filter(s=>s.status==='approved').length,mag=submissions.filter(s=>s.magazine).length;return `<section class="page"><header class="page-title"><p class="eyebrow">OWNER CONTROL</p><h1>オーナー管理画面</h1><p class="lead">未承認申請、修正依頼、承認済み、専用マガジン追加希望をまとめて確認できます。</p></header><div class="owner-summary"><div class="owner-stat"><b>${pending}</b><span>未承認申請</span></div><div class="owner-stat"><b>${creators.length}</b><span>公開参加者</span></div><div class="owner-stat"><b>${revision}</b><span>修正依頼中</span></div><div class="owner-stat"><b>${mag}</b><span>マガジン追加希望</span></div></div><div class="owner-grid"><aside class="panel owner-menu"><button class="${state.ownerTab==='pending'?'active':''}" data-tab="pending">未承認 <b>${pending}</b></button><button class="${state.ownerTab==='revision'?'active':''}" data-tab="revision">修正依頼 <b>${revision}</b></button><button class="${state.ownerTab==='approved'?'active':''}" data-tab="approved">承認済み <b>${approved}</b></button><a href="${MAGAZINE}" target="_blank" rel="noopener">専用マガジン ↗</a><a href="${RECRUIT}" target="_blank" rel="noopener">募集記事 ↗</a></aside><div class="panel"><div class="section-head"><div><p class="eyebrow">APPLICATION QUEUE</p><h2>${state.ownerTab==='pending'?'未承認申請':state.ownerTab==='revision'?'修正依頼':'承認済み'}</h2></div></div><div class="submission-list">${current.length?current.map(submission).join(''):'<div class="empty">該当する申請はありません。</div>'}</div></div></div></section>`}
function submission(s){return `<article class="submission"><img src="${s.image}" alt=""><div><h3>${esc(s.name)}</h3><p>${esc(s.title)}｜${esc(s.job)}</p><p>申請日時：${s.date}</p><p>専用マガジン追加希望：<b>${s.magazine?'あり':'なし'}</b></p><span class="status ${s.status}">${s.status==='pending'?'確認待ち':s.status==='revision'?'修正依頼中':'承認済み'}</span></div><div class="submission-actions"><button class="btn" data-review="${s.id}">内容確認</button>${s.status!=='approved'?`<button class="primary" data-approve="${s.id}">承認</button>`:''}${s.status==='pending'?`<button class="ghost" data-revision="${s.id}">修正依頼</button>`:''}${s.magazine?`<a class="btn" href="${MAGAZINE}" target="_blank" rel="noopener">マガジン追加</a>`:''}</div></article>`}
function render(){const pages={home,directory,battle,apply,owner};app.innerHTML=(pages[state.page]||home)();bind();notice.textContent=submissions.filter(s=>s.status==='pending').length}
function bind(){document.querySelectorAll('[data-page]').forEach(x=>x.onclick=()=>go(x.dataset.page));document.querySelectorAll('[data-creator]').forEach(x=>x.onclick=()=>openProfile(+x.dataset.creator));document.querySelectorAll('[data-tab]').forEach(x=>x.onclick=()=>{state.ownerTab=x.dataset.tab;render()});document.querySelectorAll('[data-approve]').forEach(x=>x.onclick=()=>changeStatus(+x.dataset.approve,'approved'));document.querySelectorAll('[data-revision]').forEach(x=>x.onclick=()=>changeStatus(+x.dataset.revision,'revision'));document.querySelectorAll('[data-review]').forEach(x=>x.onclick=()=>review(+x.dataset.review));const search=document.getElementById('search'),sort=document.getElementById('sort'),attr=document.getElementById('attr');if(search)search.oninput=e=>{state.search=e.target.value;render();setTimeout(()=>document.getElementById('search')?.focus(),0)};if(sort)sort.onchange=e=>{state.sort=e.target.value;render()};if(attr)attr.onchange=e=>{state.attribute=e.target.value;render()};const p=document.getElementById('player'),o=document.getElementById('opponent');if(p)p.onchange=e=>document.getElementById('playerView').innerHTML=fighter(creators.find(c=>c.id==e.target.value));if(o)o.onchange=e=>document.getElementById('opponentView').innerHTML=fighter(creators.find(c=>c.id==e.target.value));document.getElementById('start')?.addEventListener('click',startBattle);document.getElementById('applyForm')?.addEventListener('submit',e=>{e.preventDefault();submissions.unshift({id:Date.now(),name:'新しい申請見本',title:'送信された見本',job:'申請中クリエイター',image:'assets/creator-1.jpg',date:new Date().toLocaleString('ja-JP'),status:'pending',magazine:true});showToast('見本申請を送信しました');go('owner')})}
function openProfile(id){const c=creators.find(x=>x.id===id);modal.innerHTML=profile(c);modal.showModal();modal.querySelector('[data-close]').onclick=()=>modal.close();modal.querySelector('[data-battle]').onclick=()=>{modal.close();go('battle');setTimeout(()=>{const o=document.getElementById('opponent');o.value=id;o.dispatchEvent(new Event('change'))},50)}}
function startBattle(){const p=creators.find(c=>c.id==document.getElementById('player').value),o=creators.find(c=>c.id==document.getElementById('opponent').value);const score=c=>c.stats.攻撃+c.stats.魔力+c.stats.素早さ+c.stats.幸運-c.stats.防御/2+Math.random()*25;const win=score(p)>=score(o)?p:o,lose=win.id===p.id?o:p;modal.innerHTML=`<div class="dialog-shell"><button class="close" data-close>×</button><p class="eyebrow">BATTLE RESULT</p><h2>${esc(win.name)}の勝利！</h2><div class="battle-select"><div class="fighter"><img src="${win.images[0]}" alt=""><b>${esc(win.name)}</b></div><div class="vs">WIN</div><div class="fighter"><img src="${lose.images[0]}" alt=""><b>${esc(lose.name)}</b></div></div><div class="notice-box" style="margin-top:18px">対戦後は相手の専用ページやnote記事へ進み、新しいクリエイターを知る導線になります。</div><div class="actions"><button class="primary" data-loser>相手ページを見る</button><a class="btn" href="${lose.articles[0][1]}" target="_blank" rel="noopener">相手の記事を見る</a></div></div>`;modal.showModal();modal.querySelector('[data-close]').onclick=()=>modal.close();modal.querySelector('[data-loser]').onclick=()=>{modal.close();openProfile(lose.id)}}
function changeStatus(id,status){const s=submissions.find(x=>x.id===id);s.status=status;showToast(status==='approved'?'承認しました':'修正依頼に変更しました');render()}
function review(id){const s=submissions.find(x=>x.id===id);modal.innerHTML=`<div class="dialog-shell"><button class="close" data-close>×</button><p class="eyebrow">APPLICATION PREVIEW</p><h2>${esc(s.name)}</h2><img class="gallery-main" style="max-width:380px" src="${s.image}" alt=""><p class="sub">${esc(s.title)}｜${esc(s.job)}</p><div class="notice-box">専用マガジン追加希望：<b>${s.magazine?'あり':'なし'}</b></div><p class="lead">本番ではここに自己紹介、3枚の画像、2本の記事URL、プロフィールURL、権利同意内容を表示して審査します。</p></div>`;modal.showModal();modal.querySelector('[data-close]').onclick=()=>modal.close()}
function showToast(t){toast.textContent=t;toast.classList.add('show');clearTimeout(showToast.t);showToast.t=setTimeout(()=>toast.classList.remove('show'),2500)}
window.addEventListener('hashchange',()=>{state.page=(location.hash||'#home').slice(1);render()});render();