="featured-copy"><small>FEATURED SAMPLE OPPONENT</small><h3>${esc(o.name)}</h3><p>${esc(o.job)}</p></div></article><div class="float"><small>今週の対戦</small><b>${week}</b><small>回</small></div></div></section><div class="stat-band"><div><b>${approved().length}</b><span>公開クリエイター</span></div><div><b>${state.battles.length}</b><span>累計対戦</span></div><div><b>1→2</b><span>記事認証でおすすめ枠</span></div><div><b>承認制</b><span>オーナー公開確認</span></div></div><section class="section"><div class="section-head"><div><p class="eyebrow">WEEKLY WINNERS</p><h2>今週の勝者ランキング</h2></div><button class="btn" data-nav="battle">ランキング詳細</button></div>${rankRows('week',5)}</section><section class="section"><div class="section-head"><div><p class="eyebrow">SAMPLE OPPONENTS</p><h2>対戦相手は無名S noteの3カード</h2></div><span class="small">画像・ジョブ・レア度で勝率は変わりません</span></div><div class="creator-grid">${opponents().map(opponentCard).join('')}</div></section><section class="section"><div class="section-head"><div><p class="eyebrow">EASY ENTRY</p><h2>参加はIDだけ。記事を書けば枠が増える。</h2></div></div><div class="steps"><div class="step"><strong>1</strong><b>クリエイターIDで申請</b><span>カードや記事がなくても参加可能。最初のおすすめ記事枠は1つ。</span></div><div class="step"><strong>2</strong><b>オーナーが確認</b><span>公開前に確認。画像・ジョブは後から管理画面で追加できます。</span></div><div class="step"><strong>3</strong><b>参加記事を認証</b><span>参加記事URLが認証されると、おすすめ記事枠が2つへ増えます。</span></div></div></section><section class="section"><div class="section-head"><div><p class="eyebrow">NEW CREATORS</p><h2>新着クリエイター</h2></div><button class="btn" data-nav="directory">すべて見る</button></div><div class="creator-grid">${approved().slice(-3).reverse().map(participantCard).join('')}</div></section></section>`}
function directory(){const q=searchText.toLowerCase(),list=approved().filter(p=>[p.noteId,p.displayName].join(' ').toLowerCase().includes(q));return`<section class="page"><header class="page-title"><p class="eyebrow">CREATOR DIRECTORY</p><h1>冒険クリエイター名鑑</h1><p class="lead">名前・クリエイターIDから探せます。記事本文は転載せず、登録された公開URLだけを掲載します。</p></header><div class="toolbar"><input class="field" id="search" value="${esc(searchText)}" placeholder="名前・IDを検索"><button class="btn" data-clear>検索をクリア</button></div><div class="creator-grid" id="directoryGrid">${list.map(participantCard).join('')||'<div class="empty">該当するクリエイターはいません。</div>'}</div></section>`}
function battle(){const p=current(),eligible=p?.status==='approved';return`<section class="page"><header class="page-title"><p class="eyebrow">RANDOM CREATOR BATTLE</p><h1>冒険カード対戦</h1><p class="lead">ステータスやカード性能ではなく、公平なランダム判定。対戦相手と行動を選ぶだけです。</p></header><div class="grid cols-2"><div class="panel gold"><div class="status-line"><span class="badge ${eligible?'verified':''}">${eligible?`ランキング参加：${esc(label(p))}`:'ゲストプレイ'}</span><span class="small">${eligible?`本日 ${dailyCount(p.id)}/${state.settings.rankDailyLimit}戦を加算`:'承認後に正式ランキングへ加算'}</span></div></div><div class="panel"><div class="status-line"><b>判定確率</b><span class="tag">勝利 ${state.settings.winPercent}%</span><span class="tag">引分 ${state.settings.drawPercent}%</span><span class="tag">敗北 ${100-state.settings.winPercent-state.settings.drawPercent}%</span></div></div></div><section class="section"><div class="section-head"><div><p class="eyebrow">SELECT OPPONENT</p><h2>対戦相手を選ぶ</h2></div></div><div class="creator-grid">${opponents().map(o=>`<label class="panel ${o.id===opponentId?'gold':''}" style="padding:0;cursor:pointer"><input class="sr-only" type="radio" name="opponent" value="${o.id}" ${o.id===opponentId?'checked':''}>${opponentCard(o)}</label>`).join('')}</div></section><section class="section"><div class="section-head"><div><p class="eyebrow">SELECT ACTION</p><h2>行動を1つ選ぶ</h2></div></div><div class="action-grid">${Object.entries(ACTIONS).map(([k,a])=>`<button class="action-card ${battleAction===k?'selected':''}" data-action="${k}"><span style="font-size:28px">${a.icon}</span><b>${a.label}</b><span class="small">${a.copy}</span></button>`).join('')}</div><div class="actions"><button class="primary" data-play>この内容で対戦する</button></div></section><section class="section"><div class="section-head"><div><p class="eyebrow">RANKING</p><h2>勝者ランキング</h2></div></div><div class="tabs"><button class="tab ${rankPeriod==='week'?'active':''}" data-period="week">今週</button><button class="tab ${rankPeriod==='today'?'active':''}" data-period="today">今日</button><button class="tab ${rankPeriod==='all'?'active':''}" data-period="all">累計</button></div><div id="rankBox">${rankRows(rankPeriod,20)}</div></section></section>`}
function join(){uploadBuffers.delete('join');return`<section class="page narrow"><header class="page-title"><p class="eyebrow">JOIN THE ARCHIVE</p><h1>IDだけで参加申請</h1><p class="lead">最初に必要なのはnoteクリエイターIDだけ。カードや記事は後から追加できます。</p></header>${state.settings.recruitUrl?`<section class="panel gold" style="margin-bottom:18px"><p class="eyebrow">BEFORE ENTRY</p><h3>参加方法・企画内容を確認</h3><p class="lead">参加条件や企画内容は、無名S noteの募集記事で確認できます。</p><div class="actions"><a class="primary" href="${esc(state.settings.recruitUrl)}" target="_blank" rel="noopener">募集記事を読む</a></div></section>`:''}<form class="panel gold" id="joinForm"><div class="form-grid"><label class="form-label full"><span>noteクリエイターID（必須）</span><input class="field" name="noteId" required placeholder="例：ss_yr またはプロフィールURL"><small class="help">パスワードは不要です。</small></label><label class="form-label"><span>表示名（任意）</span><input class="field" name="displayName" placeholder="未入力ならIDを表示"></label><label class="form-label"><span>おすすめ記事①（任意）</span><input class="field" name="article1Url" placeholder="https://note.com/..."></label><label class="form-label full"><span>ひとこと（任意）</span><textarea class="field" name="intro" maxlength="180"></textarea></label><div class="full">${uploade