reventDefault();const f=new FormData(e.target),w=Number(f.get('winPercent')),d=Number(f.get('drawPercent'));if(w+d>=100)return toast('勝利と引分の合計は99%以下にしてください。');state.settings={title:String(f.get('title')),hashtag:String(f.get('hashtag')),started:String(f.get('started')),announcement:String(f.get('announcement')),rankDailyLimit:Number(f.get('rankDailyLimit'))||10,winPercent:w,drawPercent:d,profileUrl:String(f.get('profileUrl')),recruitUrl:String(f.get('recruitUrl')),magazineUrl:String(f.get('magazineUrl'))};save('サイト設定を更新');toast('設定を保存しました。');return render()}})
modal.addEventListener('click',e=>{if(e.target===modal)modal.close()});window.addEventListener('hashchange',()=>{route=location.hash.slice(1)||'home';render()});render();
